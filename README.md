# Lecture2: Java Collections

Sample code to show the importance of implementing `equals()` and `hashcode()` methods when using Java Collections.

File contained here:

`HashcodeEquals.java` \
ddd

`CollectionTest.java` \
ddd
